use_default_settings: true

general:
  debug: false

server:
  bind_address: "0.0.0.0"
  port: 8080
  secret_key: "search-assistant-local-only-change-before-sharing"
  limiter: false
  image_proxy: false

search:
  safe_search: 0
  formats:
    - html
    - json
